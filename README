Custom ELF Loader intended for pedagogic use
pour executer sur une bonne base 
il faut compiler avec la commande
autoreconf -vif
./configure
make

puis executer aussi config pour le repertoire Exemples 
cd Examples_fusion && autoreconf -i && cd .. && ./configure && make
./Options_and_debug_example -S ./Examples_fusion/exe
mple1.o